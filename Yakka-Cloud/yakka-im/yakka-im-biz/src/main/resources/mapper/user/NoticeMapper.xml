<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yakka.flex.im.core.user.mapper.NoticeMapper">

    <select id="getUnReadCountByType" resultType="com.yakka.flex.im.domain.vo.resp.friend.FriendUnreadDto">
        SELECT type, COUNT(*) as count
        FROM im_notice
        WHERE receiver_id = #{receiverId} AND is_read = #{isRead}
        GROUP BY type
    </select>
</mapper>
