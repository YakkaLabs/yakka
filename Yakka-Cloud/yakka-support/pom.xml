<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.yakka.flex</groupId>
        <artifactId>yakka-dependencies-parent</artifactId>
        <version>3.0.6</version>
        <relativePath>../yakka-dependencies-parent/pom.xml</relativePath>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>yakka-support</artifactId>
    <packaging>pom</packaging>
    <name>${project.artifactId}</name>
    <description>支撑模块</description>

    <modules>
        <!-- 监控和全链路调用监控 开发环境可以不用启动 -->
        <module>yakka-monitor</module>
        <!-- 定时任务执行器，启动该项目时，需要先启动 yakka-job（开源版） 或 yakka-job-pro -->
        <module>yakka-job-executor</module>
        <!-- 单体版 -->
        <module>yakka-boot-server</module>
    </modules>

</project>
